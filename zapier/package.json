{
  "name": "verto-zapier",
  "version": "0.1.0",
  "description": "Native Zapier integration for Verto — triggers, actions and search over the Verto developer API.",
  "private": true,
  "main": "index.js",
  "scripts": {
    "test": "mocha --recursive -t 10000 test",
    "push": "zapier push",
    "validate": "zapier validate"
  },
  "engines": {
    "node": ">=18",
    "npm": ">=8"
  },
  "dependencies": {
    "zapier-platform-core": "17.9.1"
  },
  "devDependencies": {
    "mocha": "^10.0.0",
    "should": "^13.2.3",
    "zapier-platform-cli": "^17.0.0"
  }
}
